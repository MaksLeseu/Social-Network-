/*import React from "react";
import {Header} from "./Header";
import {BrowserRouter} from "react-router-dom";

export default {
    title: 'Header',
    component: Header
}

export const Example1 = (props: any) => {
    return (
        <BrowserRouter>
            <Header isAuth={false} login={'Max'} />
        </BrowserRouter>
    )
}
export const Example2 = (props: any) => {
    return (
        <BrowserRouter>
            <Header isAuth={true} login={'Max'} />
        </BrowserRouter>
    )
}*/
export const a = 1
